import { WritePageStyle } from "./styled";

const WritePage = () => {
  // 어느 폴더에 저장할 건지 선택해야함.
  return (
    <WritePageStyle className="WritePage_wrap">여긴 글쓰기</WritePageStyle>
  );
};

export default WritePage;
